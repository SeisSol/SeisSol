
#include <cstddef>
#include <vector>
namespace seissol::solver::clustering::communication {

std::vector<void*> createComms(std::size_t count);

} // namespace seissol::solver::clustering::communication
