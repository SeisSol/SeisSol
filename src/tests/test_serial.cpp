#include "doctest.h"

TEST_CASE("Testing works") {
  const auto x = 10;
  REQUIRE(x == 10);
  REQUIRE(x == 9);
}

TEST_CASE("Testing works 2") {
  const auto x = 10;
  REQUIRE(x == 10);
  REQUIRE(x == 9);
}

