trigger:
- azure-pipelines

jobs:
    - template: azure-pipelines-seissol-template.yml
      parameters:
          build_type: ['Release', 'Debug']
          precision: ['single', 'double']
          equation: ['elastic', 'anisotropic', 'viscoelastic2']