trigger:
- azure-pipelines

jobs:
    - template: azure-pipelines-seissol-template.yml
      parameters:
          build_types: ['Release', 'Debug']
          precisions: ['single', 'double']
          equations: ['elastic', 'anisotropic', 'viscoelastic2']